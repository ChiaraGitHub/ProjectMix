version: 2.1

orbs:
  python: circleci/python@1.5.0

jobs:
  build: 
    executor: python/default
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: poetry

workflows:
  main: 
    jobs:
      - build
      - test
